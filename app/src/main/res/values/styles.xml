<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/cyan_dark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="toolbarStyle">@style/AppTheme.ToolBar</item>
        <item name="android:textColorSecondary">@color/cyan</item>
    </style>
    <style name="AppTheme.ToolBar" parent="Widget.AppCompat.Toolbar">
        <item name="android:background">@drawable/stroke_background</item>
        <item name="titleTextColor">@color/cyan</item>
        <item name="popupTheme">@style/AppTheme.ToolBar.MenuPopup</item>
    </style>
    <style name="AppTheme.ToolBar.MenuPopup" parent="@android:style/Widget.PopupMenu">
        <item name="android:itemBackground">@drawable/backgound_item_menu</item>
        <item name="android:colorBackground">#000</item>
        <item name="android:textColor">@color/cyan</item>
    </style>

</resources>
